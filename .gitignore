node_modules/
*.log
.DS_Store
Thumbs.db
.claude/

# Real patient notes — never publish identifiable patient details to this public repo.
# Keep them in a local *.local.md file (git-ignored) and in your private hand-in only.
*.local.md
