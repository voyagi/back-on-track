node_modules/
*.log
.DS_Store
Thumbs.db
.claude/

# Real patient notes / hand-in documents — never publish identifiable patient
# details to this public repo. Keep them in local *.local.* files (git-ignored:
# .local.md, .local.docx, .local.pdf) and in your private hand-in only.
*.local.*
